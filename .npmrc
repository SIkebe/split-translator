# npm configuration for split-translator

# Save exact versions (no ^ or ~ prefixes)
save-exact=true

# Use npm package lock
package-lock=true

# Security settings
audit-level=moderate

# Performance settings
prefer-offline=false

# Registry settings
registry=https://registry.npmjs.org/

# Install settings
engine-strict=true
